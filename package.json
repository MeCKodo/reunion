{
  "name": "cursor-chat-explorer-mvp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "index": "tsx src/server.ts index",
    "serve": "tsx src/server.ts serve --port 8765",
    "build:frontend": "npm --prefix frontend run build",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "tsx": "^4.19.3",
    "typescript": "^5.7.3",
    "@types/node": "^22.10.2"
  }
}
